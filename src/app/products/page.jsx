import Products from "@/components/home/Products";
import React from "react";
export const metadata = {
  title: "All Products",
};

const page = () => {
  return (
    <div>
      <Products />
    </div>
  );
};

export default page;
